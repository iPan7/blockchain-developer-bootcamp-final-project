function registerCustomer(address _customer) {

    // registers customer

};

function registerSeller(address _seller) {

    // registers seller

};

function registerVehicle(vehicle) {

    // registers vehicle to put on the market

};

function purchaseVehicle(address _customer, address _seller, amount of token) {

    // Sends funds from customer to seller account

};

function registerEcoWallet(address _ecoFund) {
    // registers ecoFund
}

modifier ecoVehicle() {
    // checks to see if the vehicle meets emission standards before determining what percentage of the funds paid go to the eco fund
    _
};